
uniform vec3 iResolution;
uniform float iTime;
uniform float iTimeDelta;
uniform float iFrame;
uniform float iChannelTime[4];
uniform vec4 iMouse;
uniform vec4 iDate;
uniform float iSampleRate;
uniform vec3 iChannelResolution[4];

out vec4 finalColor;

void main() {
  finalColor.w = 1.;

  mainImage(finalColor, (gl_FragCoord.xy + ifFragCoordOffsetUniform));
}
